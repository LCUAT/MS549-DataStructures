This application graphs a set of data points and then displays them using a linked list.

The graph structure defines the data points

The data points are added to the linked list structure and dispalyed to the user.